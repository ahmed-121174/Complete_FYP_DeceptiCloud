# DeceptiCloud — Jury Day Guide
> Step-by-Step Presentation Procedure

---

## Before Jury Enters (5 minutes early)

```bash
cd "/media/amei-302/New Volume1/SEMESTER VIII/Ahmed Fype-II"
bash Jury_presentation_final/scripts/1_START_SYSTEM.sh
```

Wait for `✅ ALL SYSTEMS ONLINE`. Then open browser → http://localhost:9000 and login.

**Keep these terminals ready (do NOT run yet):**
- Terminal 2: `bash Jury_presentation_final/scripts/2_WEB_ATTACKS.sh`
- Terminal 3: `bash Jury_presentation_final/scripts/3_DDOS_ATTACK.sh low`

---

## Presentation — 15 Minutes

### Step 1 — Introduction (2 min)
Say while pointing at the terminal output:
> *"DeceptiCloud runs 16 services — 7 real websites, 7 identical honeypot clones, an ML detection API, and a routing proxy. Every HTTP request enters through port 8080, gets classified by our ANN model, and is silently routed to either the real site or the honeypot."*

### Step 2 — Dashboard Overview (2 min)
Open http://localhost:9000

**Point at:**
- **Infrastructure Health** (right side) — all green tiles
- **Live Threat Feed** — attacks captured with badges
- **Phase 2 Panel** (top) — LLM requests/success, GAN 35 users 100% synthetic

> *"This is the defender's real-time command center. You can see every attack being classified and intercepted live."*

### Step 3 — Live Attack Demo (3 min)
**Run in Terminal 2:** `bash Jury_presentation_final/scripts/2_WEB_ATTACKS.sh`

Switch to browser → **Attack Analysis** tab. Watch new rows appear with:
- Red **SQLI** / **XSS** / **PATH TRAVERSAL** badges
- Confidence 90-100%

> *"Each attack is classified in ~38ms by our trained ANN model. Note the confidence score — 99% means the model is certain this is malicious. The attacker is routed to the honeypot, not our real banking site."*

### Step 4 — Phase 2 — LLM + GAN (3 min)
Back to **Overview** → scroll to Phase 2 panel.

**LLM Response Engine:**
> *"The first time an attacker sends a payload, our on-device LLM — gemma3:1b running locally — generates a unique HTML response making them think the attack succeeded. The response is cached for 10 minutes. Repeat attacks get instant responses from cache."*

**GAN Identities:**
> *"The 35 users in our honeypot databases were generated by our WGAN-GP model — not human-created. They have statistically realistic names, emails, and balances. We can mathematically prove they're synthetic using a watermark: multiply balance by 100, take the last digit — it's always 7."*

Show http://localhost:4001/api/users in browser.

### Step 5 — ML Models (1 min)
Click **ML Models** in sidebar.

> *"Web Attack Detector V2: ANN trained on CIC-IDS2017 — 93.97% balanced accuracy, 22 features. DDoS Detector V1: Random Forest on CIC-DDoS2019 — 99.88% accuracy, 30 network flow features."*

### Step 6 — Blockchain Ledger (1 min)
Click **Blockchain Ledger** in sidebar.

> *"Every captured attack is written to a SHA-256 blockchain. Tamper-evident: if anyone modifies even one byte of any block, the hash cascade fails and we detect it. This makes attack evidence admissible in forensic investigations."*

### Step 7 — DDoS Demo (2 min)
**Run in Terminal 3:** `bash Jury_presentation_final/scripts/3_DDOS_ATTACK.sh low`

> *"I'm now flooding the corporate site with DDoS traffic. The DDoS detector will flag it. Watch the corporate service tile turn red — then Kubernetes auto-restarts it in under 10 seconds."*

---

## If Jury Asks for Live Attack

Copy-paste in any terminal:
```bash
curl -v -A "sqlmap/1.7" \
  "http://localhost:8080/banking/search?q=1'+UNION+SELECT+username,password+FROM+users--"
```

Then show:
1. http://localhost:4001/api/users — fake GAN users attacker "stole"
2. Dashboard → Attack Analysis — new SQLI row appeared
3. Dashboard → Phase 2 — LLM success count went up

---

## Jury Q&A — Quick Answers

| Question | Answer |
|----------|--------|
| "How does it detect SQLi?" | 22-feature vector → ANN model in 38ms. `sqlmap` user-agent alone = 90% confidence |
| "What does attacker actually receive?" | GAN fake users (admin@quickbank.net with $250K balance) — mathematically proven synthetic |
| "Why dynamic responses?" | Every unique payload gets unique AI-generated "leaked data" — attacker can't fingerprint the system |
| "Can you prove it's tamper-proof?" | Blockchain page: modify one block → all hashes after it fail — visually verifiable |
| "What if DDoS crashes everything?" | K8s watchdog restarts pods in <10s — shown live in demo |
| "Is GAN data realistic?" | WGAN-GP trained on real data distributions, watermarked with balance digit pattern |
| "What's the detection rate?" | ANN: 93.97% balanced accuracy, DDoS: 99.88% — both on IEEE standard benchmarks |
| "Why not just block attackers?" | Deception is more valuable — attacker wastes time, reveals TTPs, and we log it all immutably |

---

## Emergency Restart
```bash
bash Jury_presentation_final/scripts/STOP_ALL.sh
sleep 5
bash Jury_presentation_final/scripts/1_START_SYSTEM.sh
```
